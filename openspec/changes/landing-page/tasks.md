## 1. Left Panel — Content

- [ ] 1.1 Replace h1 and intro paragraph with "One Piece Cards API" and description
- [ ] 1.2 Replace list items with: authentication instruction (Bearer token code snippet), link to `/docs/api`, reference to `/mcp`
- [ ] 1.3 Remove "Deploy now" button and Laravel version/changelog paragraph

## 2. Right Panel — SVGs

- [ ] 2.1 Replace Laravel wordmark SVG paths with SVG `<text>` element rendering "One Piece TCG"
- [ ] 2.2 Replace "13" fan-effect SVG (5 `<g>` groups with paths/masks) with SVG `<text>API</text>` in the same 5-group blend-mode structure

## 3. Cleanup

- [ ] 3.1 Remove `Route::has('login')` header block (no login route in this project)
- [ ] 3.2 Update `<title>` to "One Piece Cards API"
- [ ] 3.3 Verify dark mode, animations, and layout are intact in browser
